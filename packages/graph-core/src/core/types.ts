/**
 * Minimal core type definitions required for compilation
 */

// Placeholder exports to satisfy imports
export interface GraphNode {
  id: string;
}

export interface GraphEdge {
  id: string;
}

export interface AbstractGraph {
  // Placeholder interface
}
